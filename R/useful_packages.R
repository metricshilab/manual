# some commonly used R packages

install.packages(c(
    "foreach", "doParallel", "plyr", # parallel
    "ggplot2", "reshape2", # ggplot2
    "sandwich", "AER", "mcmc", # econometrics
    "numDeriv", "optimx", # numerical
    "tidyverse", "knitr", # tidy
    "caret", # ML
    'IRkernel' # IRkernel, much easier than before)
    )
)